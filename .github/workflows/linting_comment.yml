# DISABLED: nf-core linting comment workflow
# This workflow has been disabled as nf-core linting is not being used

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Workflow disabled
        run: echo "This workflow has been disabled"
